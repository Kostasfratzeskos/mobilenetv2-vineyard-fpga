# vivado/

Vivado-generated files are **gitignored**. Rebuild the project from a script rather than committing project dirs.

- `scripts/` - put `create_project.tcl` / `build.tcl` here
- Keep RTL sources in `../hardware/rtl`, not in the `.runs` / `.cache` / `.gen` folders.
